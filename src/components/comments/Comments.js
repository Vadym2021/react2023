import React, {useState} from 'react';

const Comments = () => {

    let[comments, setComments] = useState([]);

    fetch('https://')

    return (
        <div>

        </div>
    );
};

export default Comments;